@model IEnumerable<OnlineShopWebApp.Models.Product>

@foreach (var product in Model)
{
    <div>
        <p>@product.Id</p>
        <p>@product.Name</p>
        <p>@product.Cost</p>
        <hr />
    </div>
}