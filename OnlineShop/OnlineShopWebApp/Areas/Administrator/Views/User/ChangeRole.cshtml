@model List<Role>
<form method="post">
	<h4>Смена роли доступа для пользователя @ViewData["email"]</h4>
	<br />
	<div class="text-danger font-weight-bold" asp-validation-summary="ModelOnly"></div>
	<select class="form-select-sm" name="Role">
		<option selected>@ViewData["role"]</option>
		@foreach (var role in Model)
		{
			<option value="@role.Name">@role.Name</option>
		}
	</select>
	<br />
	<br />
	<br />
	<button type="submit" class="btn btn-primary" asp-area="Administrator" asp-controller="User" asp-action="ChangeRole" asp-route-id="@ViewData["id"]">Сохранить</button>
	<a class="btn btn-outline-primary" asp-area="Administrator" asp-controller="User" asp-action="Index">Отмена</a>
</form>